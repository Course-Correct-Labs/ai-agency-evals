# Full evaluation configuration for phi_eval
# Complete dataset with real API calls

dataset_mode: full  # Use complete dataset (~150 questions)
output_dir: outputs/phi
timeout: 30

models:
  - name: gpt-4
    provider: openai
    model: gpt-4

  - name: claude-3-5-sonnet
    provider: anthropic
    model: claude-3-5-sonnet-20241022
